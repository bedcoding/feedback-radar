import type { ReactNode } from 'react';

export const metadata = {
  title: '동작 원리 — Feedback Radar',
  description: 'Feedback Radar 내부 동작 원리 기술 슬라이드',
};

export default function DeckLayout({ children }: { children: ReactNode }) {
  return children;
}
