import type { ReactNode } from 'react';

export const metadata = {
  title: '소개 | Feedback Radar',
  description: '사용자 피드백 자동 수집, 분류, 브리핑 도구 소개 슬라이드',
};

export default function PitchLayout({ children }: { children: ReactNode }) {
  return children;
}
